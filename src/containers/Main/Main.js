import Background from "../../layout/background/background";
import Header from "../../layout/header/header";

const Main = (props) => {
	return (
		<Background main>
			<Header />
		</Background>
	);
};

export default Main;
