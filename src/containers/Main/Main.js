import { Fragment } from "react";
import Header from "../../components/header/header";

const Main = (props) => {
	return (
		<Fragment>
			<Header />
			<div></div>
		</Fragment>
	);
};

export default Main;
